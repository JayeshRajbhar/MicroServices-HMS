spring.config.import=optional:file:staff/.env[.properties],optional:file:.env[.properties]
spring.application.name=staff
server.port=${STAFF_PORT:8083}
spring.datasource.url=jdbc:postgresql://aws-1-ap-northeast-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.nlruqajyozkeavpfkxgh
spring.datasource.password=${DB_PASSWORD}
spring.datasource.hikari.maximum-pool-size=6
spring.datasource.hikari.minimum-idle=1
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
jwt.secret=${JWT_SECRET}
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
services.patient.base-url=http://patient
services.doctor.base-url=http://doctor
eureka.client.service-url.defaultZone=http://${EUREKA_HOST:localhost}:${EUREKA_PORT:8761}/eureka
eureka.instance.prefer-ip-address=true
